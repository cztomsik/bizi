{
  "name": "bizi",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "dev": "babel src/ --out-dir build/ --watch --source-maps inline",
    "prepublish": "babel src/ --out-dir build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cztomsik",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.3.17",
    "babel-preset-es2015": "^6.3.13"
  },
  "dependencies": {
    "bootstrap": "3.x",
    "jquery": "1.9.1 - 2"
  }
}
